database {
  driver = "$(DATABASE_DRIVER)"
  url = "$(DATABASE_URL)"
}
jwt.secret = "$(AUTH0_CLIENT_SECRET)"

api.persons {
  uri = "$(DAISY_API_URI)"
  username = "$(DAISY_API_USERNAME)"
  password = "$(DAISY_API_PASSWORD)"
}
